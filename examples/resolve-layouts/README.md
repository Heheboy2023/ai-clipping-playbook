# Resolve layout exercise

Build crop, pad, and stacked-speaker versions from generated fixtures using `templates/resolve/layout-presets-spec.md`. Record every change in the crop shot list and compare with Clipkit’s crop/pad outputs. No unverified Resolve preset binary is included.
