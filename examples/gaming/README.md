# Gaming clipping example

The padded vertical layout preserves the full synthetic game frame. Real gameplay may include publisher rules, licensed music, player names, overlays, chat, and third-party voice; review those elements before use.

Run: `clipkit run --manifest examples/gaming/job.yaml --dry-run`
