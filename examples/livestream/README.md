# Livestream clipping example

This workflow represents an authorized local VOD excerpt. Before choosing a real moment, preserve the surrounding conversation, chat/on-screen context, music, and source ownership in `templates/vod-context-notes.md`.

Run: `clipkit run --manifest examples/livestream/job.yaml --dry-run`
