# Sample project intake

Run `clipkit init work/sample-project`, then intake a generated fixture with explicit authorization. Compare the resulting folders and source hash with `templates/source-intake-manifest.csv` and `templates/rights-log.csv`.
