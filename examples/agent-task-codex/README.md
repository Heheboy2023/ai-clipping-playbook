# Codex fixture task

Goal: add or adjust one Clipkit behavior against generated fixtures only.

Allowed paths: the named source module, its focused test, and matching docs.  
Required: plan first, no external retrieval, no publishing, no broad deletion, targeted test, full suite, changed-file/failure report.  
Prompt: adapt `prompts/codex/task-contract.md` and keep `AGENTS.md` authoritative.
