# YouTube-style video clipping example

The input is a locally generated landscape fixture, not a downloaded YouTube video. For real work, use a creator-supplied master or another access method the rights holder has authorized. `clipkit retrieve-metadata` may record sanitized metadata after explicit authorization, but it does not download media.

Run: `clipkit run --manifest examples/youtube-video/job.yaml --dry-run`
