# Media fixture exercise

The canonical generated files live in `examples/fixtures/`. Run `python scripts/generate_fixtures.py`, probe each `sample-*.mp4`, then compare crop, pad, blur, rotation, stream-copy, and encoded outputs using `docs/ffmpeg-commands.md`.

Do not substitute downloaded creator footage for repository tests.
