# Multi-speaker podcast example

This synthetic four-panel image exercises crop and caption mechanics. It does not perform speaker diarization or active-speaker tracking. Reconcile model labels with `templates/speaker-map.csv`, create a crop shot list, and review every speaker change in real footage.

Run: `clipkit run --manifest examples/multi-speaker-podcast/job.yaml --dry-run`
