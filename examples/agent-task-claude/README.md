# Claude Code fixture task

Use the same acceptance contract as `examples/agent-task-codex/`, with `CLAUDE.md`, `.claude/settings.json`, and `prompts/claude/task-contract.md`. Begin in plan mode and verify that the output-root hook rejects an outside path.
